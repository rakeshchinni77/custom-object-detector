version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=8000
      - MODEL_WEIGHTS=models/trained/best.pt
    volumes:
      - .:/app
